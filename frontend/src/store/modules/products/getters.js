const getProducts = (state) => state.products;
const getPager = (state) => state.pager;
const getNewProducts = (state) => state.newProducts;
const getFeaturedProducts = (state) => state.featuredProducts;

export default {
  getProducts,
  getPager,
  getNewProducts,
  getFeaturedProducts,
};
